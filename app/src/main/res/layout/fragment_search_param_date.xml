<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/date_label"
        style="@style/SearchParameterHeader"
        />

    <com.squareup.timessquare.CalendarPickerView
        android:id="@+id/search_param_date_calendar"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        />

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/search_parameter_buttons_border"
        >

        <Button
            android:id="@+id/search_parameter_dismiss_button"
            android:text="Dismiss"
            style="@style/SearchParamsButton"
            />

        <View
            android:layout_width="1dp"
            android:layout_height="match_parent"
            android:background="@color/search_parameter_buttons_border"
            />

        <Button
            android:id="@+id/search_parameter_ok_button"
            android:text="Ok"
            style="@style/SearchParamsButton"
            />

    </LinearLayout>

</LinearLayout>